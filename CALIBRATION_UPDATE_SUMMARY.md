# 坐标校准工具更新总结

## 更新时间
2026-02-10

## 更新内容

### ✅ 简化校准流程

**移除的标记步骤**：
1. ~~5个仓库选择位置~~ → 使用默认配置
2. ~~滚动区域（左上角和右下角）~~ → 使用默认配置

**保留的标记步骤**：
1. ✅ 仓库网格（8列×4行，共32个点）
2. ✅ 自动存放按钮
3. ✅ 仓库切换按钮
4. ✅ 确认按钮
5. ✅ 关闭按钮
6. ✅ 8个物品分类按钮

## 更新原因

1. **减少工作量**
   - 仓库选择位置在不同游戏版本中相对固定
   - 滚动区域可以使用默认的仓库网格区域
   - 用户标记点数从约50个减少到约44个

2. **提高易用性**
   - 校准步骤从8步减少到6步
   - 预计校准时间减少20%
   - 降低出错概率

3. **保持灵活性**
   - 默认配置适用于大多数情况
   - 如需调整，可手动编辑配置文件

## 更新的文件

### 代码文件
1. ✅ `examples/calibrate_coordinates.py`
   - 移除仓库选择位置标记逻辑
   - 移除滚动区域标记逻辑
   - 简化配置文件生成逻辑

### 文档文件
2. ✅ `README.md`
   - 更新标记步骤说明
   - 添加默认配置说明

3. ✅ `QUICKSTART.md`
   - 更新快速开始指南
   - 简化标记步骤描述

4. ✅ `FILES_OVERVIEW.md`
   - 更新校准工具流程图
   - 添加注意事项

5. ✅ `config/layout_1080p.py`
   - 添加默认配置说明注释
   - 说明如何手动调整

6. ✅ `CHANGELOG.md`（新建）
   - 详细的更新日志
   - 升级指南
   - 未来计划

## 默认配置

### 仓库选择位置（默认值）

```python
@property
def WAREHOUSE_SELECT_POSITIONS(self) -> dict[str, Point]:
    return {
        "仓库1": Point(960, 300),
        "仓库2": Point(960, 380),
        "仓库3": Point(960, 460),
        "仓库4": Point(960, 540),
        "仓库5": Point(960, 620),
    }
```

**假设**：
- 仓库选择界面在屏幕中央
- 仓库选项纵向排列
- 每个选项间隔80像素

### 滚动区域（默认值）

```python
@property
def SCROLL_AREA(self) -> Region:
    return Region(Point(200, 200), Point(1400, 800))

@property
def SCROLL_CENTER(self) -> Point:
    return Point(800, 500)  # 滚动区域中心点
```

**假设**：
- 滚动区域覆盖仓库网格区域
- 中心点用于执行滚动操作

## 如何调整默认配置

如果默认配置不准确，可以手动编辑生成的配置文件：

### 方法1：直接编辑生成的配置文件

```python
# 编辑 config/layout_1080p_generated.py

# 调整仓库选择位置
@property
def WAREHOUSE_SELECT_POSITIONS(self) -> dict[str, Point]:
    return {
        "仓库1": Point(960, 300),  # 修改X和Y坐标
        "仓库2": Point(960, 380),
        "仓库3": Point(960, 460),
        "仓库4": Point(960, 540),
        "仓库5": Point(960, 620),
    }

# 调整滚动区域
@property
def SCROLL_AREA(self) -> Region:
    return Region(
        Point(200, 200),   # 左上角坐标
        Point(1400, 800)   # 右下角坐标
    )
```

### 方法2：使用截图工具确定坐标

1. 截取仓库切换界面
2. 使用图像编辑工具（如Paint）查看坐标
3. 将坐标填入配置文件

### 方法3：运行时测试

1. 使用默认配置运行一次
2. 观察点击位置是否准确
3. 根据偏差调整坐标
4. 重新运行验证

## 测试建议

### 测试默认配置

运行以下命令测试默认配置是否准确：

```bash
# 运行转移任务（测试模式）
uv run python -m endfield_essence_recognizer.warehouse_transfer.examples.run_transfer
```

**观察要点**：
1. 仓库切换时，点击位置是否准确
2. 滚动时，是否能正常查看更多物品
3. 如有偏差，记录实际位置与预期位置的差异

### 调整后验证

修改配置后，重新运行测试：

```bash
# 重新运行转移任务
uv run python -m endfield_essence_recognizer.warehouse_transfer.examples.run_transfer
```

## 兼容性

### 向后兼容

- ✅ 已有的配置文件仍然有效
- ✅ 不需要重新标记坐标
- ✅ 不影响现有功能

### 升级建议

如果你已经使用旧版本标记过坐标：
- **不需要重新标记**，旧配置继续有效
- 如果想使用新的简化流程，可以重新运行校准工具

## 常见问题

### Q1: 默认的仓库选择位置不准确怎么办？

A: 手动编辑配置文件，调整坐标。参考上面的"如何调整默认配置"部分。

### Q2: 滚动功能不正常怎么办？

A: 检查滚动区域配置，确保覆盖仓库网格区域。可以调整 `SCROLL_AREA` 和 `SCROLL_CENTER`。

### Q3: 我需要重新标记所有坐标吗？

A: 不需要。如果你已经有配置文件，继续使用即可。新版本只是简化了校准流程。

### Q4: 如何知道默认配置是否适合我的游戏？

A: 运行一次转移任务，观察仓库切换和滚动是否正常。如有问题，再调整配置。

### Q5: 可以恢复旧的校准流程吗？

A: 可以。如果需要手动标记所有位置，可以使用旧版本的校准工具，或者手动编辑配置文件。

## 反馈

如果默认配置在你的环境中不准确，请反馈：
1. 你的游戏版本
2. 实际的仓库选择位置坐标
3. 实际的滚动区域坐标

这将帮助我们改进默认配置。

---

**更新完成！** 🎉

现在校准流程更简单了，只需标记6个步骤即可完成配置。
